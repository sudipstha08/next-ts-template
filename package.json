{
  "name": "next-ts-template",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start-dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "eslint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0 --fix",
    "fix": "yarn lint:ts --fix",
    "git-add": "git add .",
    "typecheck": "tsc-files --noEmit",
    "postformat": "prettier . --write",
    "lint:css": "npx stylelint --ignore-path .gitignore '**/*.{css,js,tsx,ts}'",
    "lint-staged": "lint-staged",
    "prepare": "husky"
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18",
    "next": "14.2.4"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "eslint": "^8",
    "eslint-config-next": "14.2.4"
  }
}
